import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.flappyguy',
  appName: 'com.example.flappyguy',
  webDir: 'dist'
};

export default config;
